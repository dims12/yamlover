- Jetbrains plugins: structure window?
- Jetbrains plugins: reformat code
- ~~Disambiguate `manager: *whiskers`: local member vs global alias precedence~~ — resolved by design (ANCHOR_REFACTOR, 2026-06-12): the anchor namespace is eliminated, anchors are real keys, `*name` is pure path lookup (URIs.md §`&`); lands with PLAN Phase A
- Plugins? L markdown preview functionality lost - DONE?
- Design reverse tag into unkwonn location of an array? - DONE
- Catch up YAML standard, why tuple key caused any confusions? 
- Example 59-all-formats-object chapter sample is not migrated from old chapter format
- Support for multilingual chunks in marklower (conditional? schemas level?)
- ~~Create full scaled test corpus for yamlover, including different concretes~~ — DONE (2026-07-20): `test-examples/` — 150 fixtures (source + canonical IR + byte goldens), one harness in `tools/engine/ts/test/fixtures.test.ts`, goldens via `npm run gen:fixtures`; format spec in `test-examples/README.md`
- ~~Design a way to represent lists and other unkeyed containers as subdirectories concrete~~ — DONE (2026-07-24): the dir-seq derivation (concrete-rules.ts: an untagged ordinal container child of a dir-concrete node → an `itemNN` subdirectory + a `- *itemNN` body pointer-array element; the 56-array-of-files shape). Positional prefix + derived dimmed `&` anchors in the projection; dir-concrete chapters materialize subchapters as subdirectories (create-menu default + Tab-wrap's first body commit)
- Orphan-GC / trash affordance for item directories left behind by pointer-element removal (v1 orphans them as keyed-only members on purpose — never destroy user data)
- Generated member names carry order numbers (2026-07-24): `nextMemberName` (concrete-rules.ts) — `item01`/`01-Заголовок`, between-inserts slot sub-numbers (`item01-1`, `01-1-…`), never renumbering. Known degenerate: repeated inserts BEFORE `00` reuse the deepening path (cosmetic only). Depth: the read-view window now applies to the projectional chapter editor too (read-only inlined subchapters + auto-descend on out-of-window edits), controlled by a discrete slider (1..6, ∞)
- Unify the read-only `ListBody`/`ListItem` (list.tsx) with the projectional `ListNode` (chapter-editor/view.tsx) — deferred from the chapter-shared.tsx unification: value-driven vs mutable-cell models; only `sublistKind` is shared today
- DELETE the flat ChapterEditor next cycle (2026-07-24: the projection became the default after closing the table/latex/hash-scroll parity gaps; `?chapterEditor=flat` is the escape hatch) — removes ChapterEditor/EditChunk + the snapshot/diff sync (useChapterSync, buildChapterModel/diffChapter/snapshotChapter) and migrates the flat-editor tests
- RETIRE the legacy chapter projection (2026-07-29: the YED chapter editor became the DEFAULT after the superset parity gate passed — test/yed-chapter-parity.test.tsx; `?chapterEditor=projectional` is the one-cycle escape hatch) — Stage 9 of the port plan: delete `chapter-editor/` (view/blocks/format/tab, keep format-bus.ts) + `chapter-projection.test.tsx`, then evaluate removing `yamlover-editor/{host,model,cells,editor,ops,paste}` and the flat editor together
- yed chapter follow-ups (2026-07-29): a `{}` insert-data-chunk command (the user picks the tag); arrows crossing a SOURCE-chunk boundary (click-only today); registry-aware core positionsOf
- Positional media drop (drop BETWEEN chunks inserts the pointer at that position) — today a chapter drop appends at the body's end; the enclosing-section targeting (data-chapter-path) landed 2026-07-24