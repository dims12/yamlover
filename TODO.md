- Jetbrains plugins: structure window?
- Jetbrains plugins: reformat code
- ~~Disambiguate `manager: *whiskers`: local member vs global alias precedence~~ — resolved by design (ANCHOR_REFACTOR, 2026-06-12): the anchor namespace is eliminated, anchors are real keys, `*name` is pure path lookup (docs/language/pointers/bookmarks); lands with PLAN Phase A
- Plugins? L markdown preview functionality lost - DONE?
- Design reverse tag into unkwonn location of an array? - DONE
- Catch up YAML standard, why tuple key caused any confusions? 
- Example 59-all-formats-object chapter sample is not migrated from old chapter format
- Support for multilingual chunks in marklower (conditional? schemas level?)
- ~~Create full scaled test corpus for yamlover, including different concretes~~ — DONE (2026-07-20): `test-examples/` — 150 fixtures (source + canonical IR + byte goldens), one harness in `tools/engine/ts/test/fixtures.test.ts`, goldens via `npm run gen:fixtures`; format spec in `test-examples/README.md`
- ~~Design a way to represent lists and other unkeyed containers as subdirectories concrete~~ — DONE (2026-07-24): the dir-seq derivation (concrete-rules.ts: an untagged ordinal container child of a dir-concrete node → an `itemNN` subdirectory + a `- *itemNN` body pointer-array element; the 56-array-of-files shape). Positional prefix + derived dimmed `&` anchors in the projection; dir-concrete chapters materialize subchapters as subdirectories (create-menu default + Tab-wrap's first body commit)
- Orphan-GC / trash affordance for item directories left behind by pointer-element removal (v1 orphans them as keyed-only members on purpose — never destroy user data)
- Generated member names carry order numbers (2026-07-24): `nextMemberName` (concrete-rules.ts) — `item01`/`01-Заголовок`, between-inserts slot sub-numbers (`item01-1`, `01-1-…`), never renumbering. Known degenerate: repeated inserts BEFORE `00` reuse the deepening path (cosmetic only). Depth: the read-view window now applies to the projectional chapter editor too (read-only inlined subchapters + auto-descend on out-of-window edits), controlled by a discrete slider (1 2 4 8 16, ∞)
- Unify the read-only `ListBody`/`ListItem` (list.tsx) with the yed chapter list cells (`tools/yed/src/chapter/cells.tsx`) — deferred from the chapter-shared.tsx unification; the legacy projectional `ListNode` (chapter-editor/view.tsx) died with Stage 9, so nothing is shared today (`sublistKind` stayed in list.tsx): value-driven vs pure-machine cell models
- DELETE the flat ChapterEditor next cycle (2026-07-24: the projection became the default after closing the table/latex/hash-scroll parity gaps; `?chapterEditor=flat` is the escape hatch) — removes ChapterEditor/EditChunk + the snapshot/diff sync (useChapterSync, buildChapterModel/diffChapter/snapshotChapter) and migrates the flat-editor tests
- ~~RETIRE the legacy chapter projection~~ — DONE (2026-07-29, Stage 9): `chapter-editor/{view,blocks,format,tab}` + their suites deleted; `format-bus.ts` + `format-control.tsx` survive (the yed mount rides the same bus). And DONE (2026-08-03, the A1-A8 parity port): `yamlover-editor/{host,model,cells,editor,ops,paste}` + its four suites + the `?yedEditor=legacy` flag deleted — yed is the ONE source editor; only the flat ChapterEditor (above) remains to retire
- yed chapter follow-ups (2026-07-29): a `{}` insert-data-chunk command (the user picks the tag); arrows crossing a SOURCE-chunk boundary (click-only today); registry-aware core positionsOf
- Positional media drop (drop BETWEEN chunks inserts the pointer at that position) — today a chapter drop appends at the body's end; the enclosing-section targeting (data-chapter-path) landed 2026-07-24
- Upload `brand/yamlover-social-preview.png` at GitHub Settings → General → Social preview (2026-08-17) — a hand step, the one repo-metadata slot with no REST API and no `gh` flag. Description, homepage (`/docs`) and 15 topics are already set; the repo had none of them, so it appeared on no topic page at all
- ~~Docs SEO~~ — DONE (2026-08-17): `sitemap.xml` (the chapter spine, 293 pages), the origin's `robots.txt` from the demo front door, per-route `<title>`/description/canonical/OG injected into the shell, and a `noindex` the client sets on a dead address. What remains is off-repo: submit the sitemap in Search Console and watch whether the pages get picked up — nothing here can verify that
- Twitter card tags (2026-08-17), the one metadata family the shell still omits: `twitter:card`/`title`/`description`/`image` and an `og:image`. The social-preview PNG exists (`brand/yamlover-social-preview.png`) but no page references it, so an unfurl gets text and no picture
- GA4 key event for the demo registration (2026-08-17): the property records 0 key events, so the funnel's only conversion — the `/register` submit — is unmeasured; needs a gtag event on the landing page and marking it key in the property. For scale: since the tag went live 2026-08-07, 23 sessions / 14 users / 71 views, 91% direct + 9% github.com referral, organic search zero
- README motivation paragraph for OSINT research (2026-08-17), matching the docs front-page use-case list (8d8e7a0) — the README is what GitHub visitors actually read and carries no investigative angle
- `.jsonl` / NDJSON as a NODE STREAM (2026-08-17): today it is named `application/x-ndjson` and held verbatim as text (walk.ts's foreign-code bucket) — enough to stop the yamlover parser mangling it, but a `.jsonl` is really a SEQUENCE of documents, so it should decode to an array of nodes like the `yaml/stream` concrete does (`DOC_CONCRETES`, walk.ts): one line = one json5p document = one element, with per-line spans so a bad line degrades alone instead of the file
- A PARSE CACHE keyed by (size, mtime) (2026-08-17), the deeper half of the incremental-reindex work: the watcher now takes a subtree patch for a single edited file (`patchOne`, engine-api.ts), but every FULL walk still re-reads and re-parses every unchanged file — `readTracked` (walk.ts) has no cache and `manifestCache` short-circuits only `blob()`. Caching the parsed node per (path, size, mtime) would make the whole-tree walk near-free for the untouched majority, and would subsume the patch tier's win instead of racing it
- Name collision, unresolved (2026-08-17): a literal `"yamlover"` web search returns only yam-lover food/social accounts — the project is on no result page. The README and the docs front page both already spend a sentence disclaiming "not Yam lover". Worth deciding whether the answer is SEO alone (above) or something stronger