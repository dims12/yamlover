{
  "name": "@yamlover/engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "yamlover engine (TS): SQLite-backed graph store, directory walker, pointer resolver, watcher, mediated mv, query evaluator.",
  "scripts": {
    "test": "node --test test/*.test.ts"
  },
  "engines": {
    "node": ">=22.13"
  },
  "dependencies": {
    "xxhash-wasm": "^1.1.0"
  }
}
