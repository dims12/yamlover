# A TASK (chore) — a chapter (the title as the node's SELF-VALUE, a keyed description, and a
# positional body of chunks/subtasks) plus planning fields. Its lifecycle STATE is a tag
# application: the `&…:-` membership bookmark into the dev workflow. Move it across the
# board by changing that one pointer. Spec: ../../TICKETS.md §1.
!!<*yamlover:$defs:task>
Fix the leaky tap
description: The bathroom tap drips overnight — replace the washer, or the whole cartridge.
priority: high
assignee: dims
estimate: 2h
- Turn the water off at the small valve under the sink FIRST.
- A half-inch washer kit is in the garage toolbox, middle drawer.
&::yamlover:ontos:colors:yellow:-
&::yamlover:ontos:workflow:dev:ready:-
