$defs:
  rectangular-area:
    type: object
    properties:
      x:
        type: integer
      y:
        type: integer
      dx:
        type: integer
      dy:
        type: integer
properties:
  object_detection.png:
    type: binary
    format: image/png
    x-yamlover:
      concrete: file/binary
  markup:
    type: array
    prefixItems:
      - description: bus
        $ref: '#/$defs/rectangular-area'
        properties:
          x:
            const: 25
          y:
            const: 40
          dx:
            const: 25
          dy:
            const: 40
      - description: car
        $ref: '#/$defs/rectangular-area'
        properties:
          x:
            const: 25
          y:
            const: 40
          dx:
            const: 25
          dy:
            const: 40
